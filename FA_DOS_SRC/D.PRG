param _why
 if pcount()==0
   ?"sales tax person is around"
  elseif _why=="@#!"
    ?"show actual details"
  else
     ?"sales tax person is around:" 
 endif

